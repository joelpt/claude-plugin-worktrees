{
  "name": "worktrees",
  "version": "2026.05.28.3",
  "description": "Surfaces a repo's mergeable git worktrees (those with no live claude session) at session start and lands chosen ones into the default branch by rebase + fast-forward via a deterministic engine, with confidence-gated conflict handling, post-land tests, and exact-state rollback. Repo-scoped — never looks cross-repo.",
  "author": { "name": "Joel Thornton" }
}
